import funcUrls from "../../backend/func2url.json";

export const API = {
  googleAuth: funcUrls["google-auth"],
  questions: funcUrls["questions-api"],
  answers: funcUrls["answers-api"],
  votes: funcUrls["votes-api"],
};

export default API;
